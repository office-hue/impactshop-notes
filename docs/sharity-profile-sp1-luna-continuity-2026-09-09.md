# Sharity profile SP1 — Luna continuity checkpoint

- **Repository/base:** `impactshop-notes`, fresh `origin/main@a6f83e37566d6e44183e8be5a5459b8461c64e93`
- **Branch/worktree:** `dev/sharity-profile-sp1-20260909` / dedicated clean worktree
- **Package:** identity-v2 bounded source package
- **Implemented:** no automatic code generation on profile render; safe profile API contract; nonce-protected code generation; one-time first-party reveal page; hashed v2 code storage with legacy verification/migration; generic sign-in errors and per-profile rate limit; Hungarian sign-in copy; profile-path AdSense exclusion.
- **Tests:** `python3 tests/impactshop-identity-profile-v2-static.test.py` PASS; `node --check wp-content/mu-plugins/impactshop-identity-panel.js` PASS; VPS dev `php -l` PASS for both changed PHP files; `git diff --check` PASS.
- **Not run:** production/provider/VPS deployment, database migration, full live E2E. Those require the Sol release gate and operator approval.
- **Terra QA:** `e577270` was not publishable because code generation trusted the client-writable pseudo cookie as ownership.
- **Sol decision:** pseudo ID and logged-out WordPress nonce are never ownership proof; see `docs/sharity-profile-sp1-owner-grant-sol-decision-2026-09-09.md`.
- **Luna correction:** current working tree adds the device-grant table/verifier, HTTPS-only `__Host-impactshop_owner` cookie, owner-bound code generation, legacy-code grant minting, same-origin mutation checks, and profile mutation gating.
- **Current evidence:** identity-v2 static PASS; JS syntax PASS; VPS PHP lint PASS; `git diff --check` PASS. No live DB migration or staging E2E.
- **Terra QA1:** `608469b` is not release-ready: account switch via `identity/restore` issues a replacement owner grant but does not revoke the prior device grant. See `docs/sharity-profile-sp1-terra-qa1-2026-09-09.md`.
- **Luna correction:** current working tree revokes the current grant before restore issues a replacement grant and adds a static ordering assertion.
- **Terra re-review:** `f26df98` closes QA1's account-switch finding by invoking current-grant revocation before replacement-grant issuance; static contract PASS.
- **Sol release gate:** `0a47559` was reviewed with an empty checkpoint delta. The repo-local DEV-v2 guard returns `operator-review / protected-or-deploy`; the release matrix keeps `impactshop-notes` source-only without a separate live grant. No remote, provider, database, scheduler or deployment effect was started. See `docs/sharity-profile-sp1-sol-release-gate-2026-09-09.md`.
- **Sol local evidence:** exact-file deploy guard PASS; deploy-map bastion PASS; DEV-context policy fixture PASS. The runtime source is unchanged since the recorded identity-v2 static, JS syntax and VPS PHP-lint PASS evidence.
- **Staging preflight:** one exact-file `DRY_RUN=1` attempt for `impactshop-identity-panel.php` stopped locally before SSH/rsync because the canonical guard requires `main`, while the frozen package is on `dev/sharity-profile-sp1-20260909`. No remote effect; no retry and no guard bypass. The remaining two previews were not started.
- **Publication candidate:** the never-pushed local checkpoint history was safely repacked because the push guard rejected its original mixed protected/app-content commit. Backup ref: `backup/sharity-profile-sp1-pre-publication-20260909@837a053`. Identity source and test are `6f7aa97`; the separate AdSense lane is `3ead4ee`; maximum-bastion digests are `1ca1fb0`. Runtime bytes are unchanged from the reviewed package.
- **Publication preflight:** refreshed `origin/main` is still the frozen base `a6f83e3`; identity static, JS syntax, exact-file guard, deploy bastion, DEV-context fixtures, guard-manifest checksum and local PR checklist validation PASS. VPS PHP-lint evidence is reused because both PHP source blobs and the VPS dev environment identity are unchanged.
- **Guarded-push preflight 1:** no GitHub write occurred. Commit-lane and protected-touch passed; strict audit stopped because the bástya status update lacked its governance system-plan sync. That evidence-only sync is now included without changing guard semantics or shared hooks.
- **Guarded-push preflight 2:** no GitHub write occurred. Commit-lane, protected-touch and strict audit passed, then the old wrapper attempted a cross-repository central memory check. Current DEV-v3 authority classifies that check, including `worktree-shared-deps.sh check --node-only`, as `not-applicable` in `impactshop-notes`; it must not be retried through an `ai-agent` sibling. No bypass, install, shared-tree repair or third push attempt was made; the remote feature branch remains absent.
- **Main integration:** one refresh confirmed `origin/main@a6f83e3`; the exact merge was `Already up to date`, with the feature branch seven commits ahead and zero behind. No conflict or tree change occurred.
- **Repo-local readiness:** `worktree-task-start-guard.sh --json` is `allowed`. `worktree-readiness-check.sh --json` correctly remained blocked on protected-source operator review and the missing continuity pre-push binding; there is no `tsx` dependency in this path. The repository-local DEV-v2 adapter is the applicable admission mechanism, and the exact protected-source manifest now binds the approved plan, six protected paths and rollback/smoke scope.
- **Protected-source admission:** checkpoint `fcf770d` / tree `bd2b675` passed repo-local full validation, maximum bastion and candidate freeze/verify with `sourceMergeAdmission=true`. Targeted tests, push-mode continuity, strict audit and diff check remained green.
- **Publication policy clarification:** no push was attempted from `fcf770d`. Checkpoint `87f4567` incorrectly treated the absence of a newer `impactshop-notes/main` commit as proof that DEV-v3 was not active. The operator confirmed that the new global DEV rule and handbook are already installed. Under that active rule, the `ai-agent` node/dependency path is `not-applicable` here; source publication relies on the explicitly executed `impactshop-notes` task-start, continuity, audit and DEV-v2 admission chain. No sibling worktree or shared dependency repair is used.
- **Cronos/watchdog:** not required. Owner-grant expiry is authorization-time enforced and cleanup is request-driven.
- **Next:** complete the single source-only push/PR/merge cycle using the exact frozen candidate and the repo-local guard evidence. Staging schema/browser work and production remain separate Sol gates. No `ai-agent` dependency repair is part of this package.
