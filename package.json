{
  "name": "impactshop-notes-harvester",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "playwright:install": "playwright install --with-deps",
    "playwright:harvest": "tsx tools/playwright/harvester-runner.ts",
    "playwright:harvest:config": "tsx tools/playwright/harvester-runner.ts --config tools/playwright/harvester-config.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "tsx": "^4.7.0"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.3.4"
  }
}
